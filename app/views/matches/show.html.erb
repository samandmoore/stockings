<% if @match.started? %>
    <%= render partial: 'standings' %>
<% else %>
    <%= render partial: 'join' %>
<% end %>

<% unless @match.started? || current_user.matches.include?(@match) %>
    <%= link_to 'Join!', match_entries_path(@match), method: :post, class: 'btn join-match' %>
<% end %>
