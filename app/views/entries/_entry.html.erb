<li>
    Entry for <%= entry.user.full_name %>
    <ul>
        <li>
            Tech Ticker: <%= entry.technology_ticker.try(:name) || "Not picked" %>
        </li>
    </ul>

    <% if entry.editable? %>
        <%= link_to("Edit", edit_match_entry_path(entry.match, entry)) %>
    <% else %>
        <%= link_to("View", match_entry_path(entry.match, entry)) %>
    <% end %>
</li>
